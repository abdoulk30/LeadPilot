# Pinned to what was verified available on PyPI. Step 1 pins are dated
# 2026-07-09; Step 2 additions are added as each tool needs them, not
# all at once up front.

# Web framework / dashboard (Decision 022)
fastapi==0.139.0
uvicorn[standard]==0.51.0
jinja2==3.1.6
python-multipart==0.0.32
itsdangerous==2.2.0

# State store (Decision 022 — Postgres via Neon in prod; see
# scripts/devdb.sh for the local-dev equivalent)
sqlalchemy==2.0.51
alembic==1.18.5
psycopg[binary]==3.3.4

# Config / validation
pydantic-settings==2.14.2

# Rep-session auth (Decision 013). Using bcrypt directly, not passlib
# — verified passlib 1.7.4 is incompatible with bcrypt>=4.1 (it reads
# a bcrypt.__about__ attribute that no longer exists), and passlib is
# unmaintained, so pinning an old bcrypt to work around it would just
# defer the problem. bcrypt's own hashpw/checkpw API is small enough
# not to need a wrapper library.
bcrypt==5.0.0

# Google Sheets connector (PRD v1.04 section 3e), per-rep OAuth
# (Decision 026)
google-api-python-client==2.198.0
google-auth-oauthlib==1.4.0
google-auth-httplib2==0.4.0

# Encrypts rep_google_credentials.refresh_token_encrypted at the
# application layer (leadpilot/crypto.py, Decision 026's refresh-token
# storage). Was already present transitively via google-auth; pinned
# explicitly now that this repo imports it directly.
cryptography==49.0.0

# Slack Web API (Decision 022) — dispatch_slack_handoff
# NOTE: version pin is a best guess, not verified against live PyPI —
# this sandbox can't reach the network to check. If `pip install`
# fails on this line, bump to whatever's actually current.
slack_sdk==3.33.0

# Testing
pytest==9.1.1
pytest-asyncio==1.4.0
httpx==0.28.1
