# Pinned to what was verified available on PyPI on 2026-07-09.
# Scoped to Step 1 (leadpilot-docs/mvp/README.md build order) only —
# Claude Agent SDK, Twilio, and slack_sdk are Step 2 (the tools) and
# not added until that work starts.

# Web framework / dashboard (Decision 022)
fastapi==0.139.0
uvicorn[standard]==0.51.0
jinja2==3.1.6
python-multipart==0.0.32
itsdangerous==2.2.0

# State store (Decision 022 — Postgres via Neon in prod; see
# scripts/devdb.sh for the local-dev equivalent)
sqlalchemy==2.0.51
alembic==1.18.5
psycopg[binary]==3.3.4

# Config / validation
pydantic-settings==2.14.2

# Rep-session auth (Decision 013)
passlib[bcrypt]==1.7.4
bcrypt==5.0.0

# Google Sheets connector (PRD v1.04 section 3e)
google-api-python-client==2.198.0
google-auth-oauthlib==1.4.0
google-auth-httplib2==0.4.0

# Testing
pytest==9.1.1
pytest-asyncio==1.4.0
httpx==0.28.1
